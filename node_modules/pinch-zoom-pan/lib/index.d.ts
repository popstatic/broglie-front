interface IOptions {
    element: HTMLElement;
    minZoom?: number;
    maxZoom?: number;
    captureWheel?: boolean;
}
export declare function create({ element, minZoom, maxZoom, captureWheel }: Readonly<IOptions>): () => void;
export {};
